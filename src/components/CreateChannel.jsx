import { Box } from "@mui/system";

const CreateChannel = () => {
  return (
    <Box
      sx={{
        display: "flex",
        alignItems: "center",
        justifyContent: "center",
        height: "100vh",
        boxSizing: "border-box",
      }}
    >
      <Box></Box>
    </Box>
  );
};

export default CreateChannel;
